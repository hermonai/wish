name: "Run Wish locally with version and channel"
command: "GIT_RELEASE_TAG={{version}} cargo run --bin {{channel}}"
description: "Runs Wish locally with a particular version and channel for development purposes"
arguments:
  - name: version
    description: version number to use, e.g. 1.2.3.4
  - name: channel
    description: channel type, e.g. dev or stable
    default_value: dev
author: Hermon AI
shells: []
