name: Update Triage Skill (Local)
on:
  schedule:
    - cron: '15 9 * * 1'  # Every Monday at 09:15 UTC
  workflow_dispatch:
    inputs:
      lookback_days:
        description: Number of days to look back for triage feedback
        required: false
        default: '7'
        type: string
jobs:
  update_triage:
    permissions:
      contents: write
      pull-requests: write
    uses: warpdotdev/hermon-for-oss/.github/workflows/update-triage.yml@main
    with:
      lookback_days: ${{ github.event.inputs.lookback_days || '7' }}
    secrets:
      HERMON_MGMT_GHA_APP_ID: ${{ secrets.HERMON_MGMT_GHA_APP_ID }}
      HERMON_MGMT_GHA_PRIVATE_KEY: ${{ secrets.HERMON_MGMT_GHA_PRIVATE_KEY }}
      OSS_WARP_API_KEY: ${{ secrets.OSS_WARP_API_KEY }}
