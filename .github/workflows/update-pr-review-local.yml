name: Update PR Review Skill (Local)
on:
  schedule:
    - cron: '0 9 * * 1'  # Every Monday at 09:00 UTC
  workflow_dispatch:
    inputs:
      lookback_days:
        description: Number of days to look back for PR feedback
        required: false
        default: '7'
        type: string
jobs:
  update_pr_review:
    permissions:
      contents: write
      pull-requests: write
    uses: warpdotdev/hermon-for-oss/.github/workflows/update-pr-review.yml@main
    with:
      lookback_days: ${{ github.event.inputs.lookback_days || '7' }}
    secrets:
      HERMON_MGMT_GHA_APP_ID: ${{ secrets.HERMON_MGMT_GHA_APP_ID }}
      HERMON_MGMT_GHA_PRIVATE_KEY: ${{ secrets.HERMON_MGMT_GHA_PRIVATE_KEY }}
      OSS_WARP_API_KEY: ${{ secrets.OSS_WARP_API_KEY }}
